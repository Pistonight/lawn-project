version: '3'

includes:
  ccpp:
    taskfile: ../../mono-dev/task/ccpp.yaml
    internal: true
    optional: true

tasks:
  check:
    - task: ccpp:fmt-check
      vars:
        SOURCE: src
  fix:
    - task: ccpp:fmt-fix
      vars:
        SOURCE: src

  fix-srcdrop:
    - task: fix

  build:
    sources:
      - bass/bin/bass.dll
    generates:
      - ../../target/lawn/debug/out/bass.dll
      - ../../target/lawn/release/out/bass.dll
    cmds:
      - silent: true
        cmd: |
          mkdir -p ../../target/lawn/debug/out;
          mkdir -p ../../target/lawn/release/out;
          cp bass/bin/bass.dll ../../target/lawn/debug/out/bass.dll;
          cp bass/bin/bass.dll ../../target/lawn/release/out/bass.dll;
          echo '==> copied lib dlls';
